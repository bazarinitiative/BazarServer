{
    "AntiSpam": {
        //max count of user command base on ip.
        //format: "max_count, seconds"
        "Client.Command": [ "20, 5", "100, 100" ],
        //max count of user query base on ip
        "Client.Query": [ "50, 5" ]
    },

    "Logging": {
        "LogLevel": {
            "Default": "Information",
            "Microsoft": "Warning",
            "Microsoft.Hosting.Lifetime": "Information"
        }
    },
    "AllowedHosts": "*"
}
